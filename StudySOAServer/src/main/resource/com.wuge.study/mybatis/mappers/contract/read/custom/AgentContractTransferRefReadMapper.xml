<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.manyi.iw.contract.soa.server.mybatis.dao.contract.read.custom.AgentContractTransferRefReadDao">

    <select id="countUncomplateTransfer" resultType="java.lang.Integer">
        SELECT
        COUNT(*)
        FROM
        iw_agent_contract_transfer_ref AS ref
        JOIN iw_agent_contract_transfer AS transfer
        ON ref.transferId = transfer.id
        WHERE ref.billId =#{billId}
        AND state =1
        AND STATUS =1
    </select>
</mapper>