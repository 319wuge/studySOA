<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.manyi.iw.contract.soa.server.mybatis.dao.read.custom.AgentContractTransferReadDao">

	<select id="selectTransfersByBillId" resultType="com.manyi.iw.contract.soa.api.model.po.mbg.AgentContractTransfer">
		SELECT
			tf.*
		FROM
			iw_agent_contract_transfer tf
		LEFT JOIN iw_agent_contract_transfer_ref ref ON ref.transferId = tf.id
		WHERE
			ref.billId = #{billId}
		AND tf.state = 2
		AND tf.status = 1
		AND tf.type IN (4,5)
	</select>

</mapper>